// Web shim for react-native-reanimated worklets version validation
// This is a no-op on web since worklets are not used

const validateWorkletsVersion = () => {
  // No-op on web
};

export default validateWorkletsVersion;
