// Web stub for react-native-background-actions
const BackgroundService = {
  start: async () => {
    console.warn('BackgroundService.start() - not available on web');
  },
  stop: async () => {
    console.warn('BackgroundService.stop() - not available on web');
  },
  updateNotification: async () => {
    console.warn('BackgroundService.updateNotification() - not available on web');
  },
  isRunning: () => false,
};

export default BackgroundService;
