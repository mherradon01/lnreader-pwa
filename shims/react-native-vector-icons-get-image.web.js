// Web stub for @react-native-vector-icons/get-image
// This module is used by react-native-vector-icons but is not needed on web

export default function getImageLibrary() {
  // On web, we don't need a native image library
  // Vector icons are rendered as SVG or web fonts
  return null;
}
